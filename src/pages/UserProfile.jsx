import React from 'react';

const UserProfile = () => {
  return (
    <div className="container" style={{ padding: '4rem 2rem', textAlign: 'center' }}>
      <h2 className="text-gradient">User Preferences</h2>
      <p style={{ color: 'var(--text-secondary)' }}>Manage your profile and settings here (Mock View).</p>
    </div>
  );
};

export default UserProfile;
